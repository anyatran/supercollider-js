--- Download SuperCollider (current v3.11.2) at https://supercollider.github.io/download
-- Install and run once

--- Copy Quarks folder contents into a folder, for example:
~/Art/AV/SuperCollider/runningDir/Quarks

--- Copy Extensions folder content into:
~/Library/Application Support/SuperCollider/Extensions/  // on Mac
- To make sure, run (cmd + return on line) in SC: Platform.userExtensionDir

--- In SC:
-- Open startup.scd (File -> Open startup file) and copy:

Quarks.addFolder("~/Art/AV/SuperCollider/runningDir/Quarks");  // or whatever you chose
Server.local.volume.volume = -28;
Server.local.options.sampleRate = 48000;  // you can try with 96000 if your CPU can handle it, sounds better!
Server.local.options.memSize = 50*32768;
Server.local.options.numAudioBusChannels = 4*1024;
Server.local.latency=0.045;
Server.local.options.maxSynthDefs = 1024*64;
Safety(Server.local).numChannels = 2;
Safety(Server.local).defName = \safeSoft;
Safety.setLimit(0.7);  // for your ears just in case, increase if sound is distorted

-- Language -> Reboot Interpreter

-- Boot a server with cmd + B (sometimes have to repeat)

-- Execute each block of TestingMood_v0.0.1_20210726.scd in order (cmd + return on each)
